
// initial state
const initialState = {
    showIcons: true,
    recipes: []
};


// reducers update state 
const reducers = (state = initialState, action) => {
    switch (action.type) {

        default:
            return state;
    }
};

export { reducers };
export default reducers;